doctype html
html
    head
        title= title

        //get bootstrap CSS
        link(rel='stylesheet', href='/stylesheets/bootstrap.min.css')

        //get Metro Icon
        link(rel='icon' href='images/los-angeles-metro-logo.png')

        //get JQuery JS
        script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js')

        //get bootstrap JS
        script(src='https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js')

        //get c3 CSS
        link(href='stylesheets/c3.css' rel='stylesheet' type='text/css')

        // get d3 JS and c3 JS
        script(src='javascripts/d3.v3.min.js' charset='utf-8')
        script(src='javascripts/c3.min.js')

        //get plotly JS
        script(src='https://cdn.plot.ly/plotly-latest.min.js')


        //get math JS
        script(src='https://cdnjs.cloudflare.com/ajax/libs/mathjs/5.2.3/math.js')

        //get PapaParse JS
        script(src='javascripts/papaparse.min.js')

        //get the JS file where we analyze the data
        script(src='javascripts/charts.js')


        style.
            /* Set the size of the div element that contains the map */

            #map1 {
                height: 300px; /* The height is 300 pixels */
                width: 60%; /* The width is 60% the width of the web page */
            }

            #map2 {
                height: 300px; /* The height is 300 pixels */
                width: 60%; /* The width is 60% the width of the web page */
            }

            #map3 {
                height: 300px; /* The height is 300 pixels */
                width: 60%; /* The width is the 60% width of the web page */
            }

            #map4 {
                height: 300px; /* The height is 300 pixels */
                width: 60%; /* The width is 60% the width of the web page */
            }

            /* Set the style of the button that contains my information */
            #myBtn {
                display: none;
                position: fixed;
                bottom: 20px;
                left: 30px;
                z-index: 99;
                font-size: 18px;
                border: none;
                outline: none;
                background-color: lightblue;
                color: white;
                cursor: pointer;
                padding: 15px;
                border-radius: 4px;
            }

            #myBtn:hover {
                background-color: #555;
            }




    body
        block content
